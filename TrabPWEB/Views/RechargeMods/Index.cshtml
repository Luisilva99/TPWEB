@model IEnumerable<TrabPWEB.DAL.RechargeMod>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RechargeModName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RechargeModDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RechargeModName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RechargeModDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RechargeModId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RechargeModId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RechargeModId })
        </td>
    </tr>
}

</table>
